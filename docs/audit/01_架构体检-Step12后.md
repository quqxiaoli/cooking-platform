# 架构体检报告 · Step 12 完成后 / Step 13 上线前

**审计时间**：2026-05-16  
**审计目的**：Step 13（生产基础设施：EventBus 切 RabbitMQ、主从复制、Nginx、Prometheus）上线前的健康检查  
**审计人**：Claude Code  
**结论**：无红灯阻断项，有 3 个中等严重度问题需在 Step 13 开工前解决，其余为后续步骤的计划性债务

---

## 目录

1. [硬编码该进配置](#1-硬编码该进配置)
2. [耦合与解耦点](#2-耦合与解耦点)
3. [重复代码](#3-重复代码)
4. [MVP 临时妥协清单](#4-mvp-临时妥协清单)
5. [一致性问题](#5-一致性问题)
6. [Step 13 前必须处理清单](#6-step-13-前必须处理清单)

---

## 1. 硬编码该进配置

### 1.1 Consumer 批量/冲刷参数

| 严重度 | 文件:行 | 现状 | 建议 | 建议处理步骤 |
|--------|---------|------|------|------------|
| **中** | `internal/consumer/like_consumer.go:82` | `const likeBatchSize = 50` 硬编码在代码里 | 挪入 `MQConfig`（见下方配置结构建议） | Step 13 开工前 |
| **中** | `internal/consumer/like_consumer.go:85` | `const likeFlushInterval = 3 * time.Second` 硬编码 | 同上 | Step 13 开工前 |
| **中** | `internal/consumer/like_consumer.go:91` | `const likeChannelBuf = likeBatchSize * 4` 硬编码（乘数 4 也是魔数） | 同上，buffer 倍数也加入配置 | Step 13 开工前 |
| **中** | `internal/consumer/pv_consumer.go:57` | `pvBatchSize = 100` 硬编码 | 同上 | Step 13 开工前 |
| **中** | `internal/consumer/pv_consumer.go:58` | `pvFlushInterval = 5 * time.Second` 硬编码 | 同上 | Step 13 开工前 |
| **中** | `internal/consumer/count_consumer.go:85` | `countBatchSize = 20` 硬编码 | 同上 | Step 13 开工前 |
| **中** | `internal/consumer/count_consumer.go:86` | `countFlushInterval = 10 * time.Second` 硬编码 | 同上 | Step 13 开工前 |

**原因**：Step 13 切换为 RabbitMQ 后，消费语义变为 at-least-once + ACK，批量窗口需要
在生产压测后调参；硬编码在源码里意味着每次调参都要改代码 + 重新部署，而非改配置重启。

**建议新增配置段**（`pkg/config/config.go` + `configs/config.yaml`）：

```go
// MQConsumerConfig holds per-consumer tuning knobs.
type MQConsumerConfig struct {
    LikeBatchSize      int           `mapstructure:"like_batch_size"`
    LikeFlushInterval  time.Duration `mapstructure:"like_flush_interval"`
    PVBatchSize        int           `mapstructure:"pv_batch_size"`
    PVFlushInterval    time.Duration `mapstructure:"pv_flush_interval"`
    CountBatchSize     int           `mapstructure:"count_batch_size"`
    CountFlushInterval time.Duration `mapstructure:"count_flush_interval"`
}
```

挂在现有 `MQConfig` 下：`Consumer MQConsumerConfig \`mapstructure:"consumer"\``

---

### 1.2 Cache TTL 硬编码

| 严重度 | 文件:行 | 现状 | 建议 | 建议处理步骤 |
|--------|---------|------|------|------------|
| **中** | `internal/cache/like_cache.go:72` | `const likeStateTTL = 7 * 24 * time.Hour`（7天） | 挪入 `RedisConfig` 或独立 `CacheConfig` | Step 13 |
| **中** | `internal/cache/feed_cache.go:90` | `FeedCacheTTL = 5 * time.Minute` | 同上 | Step 13 |
| **中** | `internal/cache/feed_cache.go:95` | `PVDedupTTL = 1 * time.Hour` | 同上 | Step 13 |
| **低** | `internal/cache/user_cache.go:114,121` | `24*time.Hour`（SMS 每日窗口）以字面量传入 | 可挪入已有 `RatelimitConfig`，如 `SMSDailyWindow` | Step 14+ |

---

### 1.3 Handler 超时硬编码

| 严重度 | 文件:行 | 现状 | 建议 | 建议处理步骤 |
|--------|---------|------|------|------------|
| **低** | `internal/handler/health.go:50` | `context.WithTimeout(c.Request.Context(), 3*time.Second)` | 挪入 `ServerConfig.HealthCheckTimeout` | Step 14+ |

---

## 2. 耦合与解耦点

### 2.1 EventBus 抽象干净度（Step 13 重点）

**结论：接口设计合格，Step 13 切换 RabbitMQ 无需改 service 层，只需实现接口。**

具体评估：

| 评估点 | 文件:行 | 状态 | 说明 |
|--------|---------|------|------|
| `EventPublisher` / `EventSubscriber` 接口定义 | `internal/event/types.go:1-28` | ✅ 干净 | service 只注入 Publisher，consumer 只注入 Subscriber；接口窄 |
| LikeService 依赖 `event.EventPublisher` | `internal/service/like_service.go:76` | ✅ 接口 | 不依赖具体实现 |
| AuditConsumer 依赖 `event.EventBus` | `internal/consumer/audit_consumer.go:63` | ⚠️ 需双向 | AuditConsumer 需要 Subscribe（收审核请求）+ Publish（发审核结果），因此必须拿 EventBus；可接受，但切 RabbitMQ 时要确认 RabbitMQ 实现同时满足两个接口 |
| RabbitMQ 占位 | `cmd/server/main.go:495-496` | ✅ 预留 | `case "rabbitmq": return nil, errors.New("...not implemented yet")` 是正确的 fail-fast 占位 |
| Topic 常量（非 iota）| `internal/event/types.go:9-18` | ✅ 适合 RabbitMQ | 字符串 Topic 可直接用作 RabbitMQ routing key，无需转换 |

**Step 13 实现清单**（接口稳定，只需新文件）：
1. `internal/event/rabbitmq.go` — 实现 `EventBus` 接口
2. `cmd/server/main.go:496` — 替换占位错误为真实初始化

---

### 2.2 跨层调用检查

| 严重度 | 文件:行 | 现状 | 结论 |
|--------|---------|------|------|
| **无** | `internal/handler/` | handler 只调 service，不直接碰 repo | ✅ 无违规 |
| **低（可接受）** | `internal/handler/health.go:23-24` | health handler 直接持有 `*gorm.DB` + `*redis.Client` | 健康检查天然需要探针基础设施，不走 service 是正确设计 |
| **低（可接受）** | `internal/middleware/ratelimit.go:58-67` | 限流中间件直接用 Redis client | 原子性要求决定了此处不能走 service 层 |

---

### 2.3 Service 层依赖具体实现排查

所有 service 构造函数均注入接口，无具体实现泄漏：

| Service | 注入类型 | 文件:行 |
|---------|---------|---------|
| LikeService | `repository.PostRepository`（接口） | `like_service.go:85` |
| PostService | `repository.PostRepository`（接口） | `post_service.go:60` |
| UserService | `repository.UserRepository`（接口） | `user_service.go:62` |
| SearchService | `repository.PostRepository`（接口） | `search_service.go:~50` |

**无违规项。**

---

## 3. 重复代码

### 3.1 Consumer drainLoop 模式三处重复

| 严重度 | 位置 | 重复内容 |
|--------|------|---------|
| **中** | `like_consumer.go:226-247` | `drainLoop:` + `select default: break drainLoop` + `zap.L().Info("xxx consumer drained", ...)` |
| **中** | `pv_consumer.go:135-150` | 同上，代码注释明确写"Mirrors LikeConsumer's drain pattern" |
| **中** | `count_consumer.go:273-285` | 同上 |

**现状**：三个 consumer 的 drain 骨架逻辑几乎一字不差，靠注释说"参考 LikeConsumer 模式"。
注释能说清意图，但不能防止三处不一致演进（比如后续某处加了超时而另外两处没加）。

**建议**：Step 13 新增 RabbitMQ Consumer 时，顺手提取 `drainChannel[T any](ch <-chan T, flush func([]T))` 泛型 helper 到 `internal/consumer/helper.go`，三处 drainLoop 都收编进来。不阻断 Step 13，但应在 Step 13 内完成（新实现不能再复制粘贴第四遍）。

---

### 3.2 已正确复用的地方（无需改动）

| 复用点 | 文件:行 |
|--------|---------|
| `AuthorAssembler` 被 PostService + SearchService 共享 | `post_service.go:136` |
| `incrAndCheck` 被 SMS Phone + IP 限流共享 | `user_cache.go:114,121` |
| `count_consumer.go` 提取了 `send()` helper 收拢 Follow/Unfollow delta | `count_consumer.go:210` |

---

## 4. MVP 临时妥协清单

### 4.1 来自 progress 文件的偏离点

| 严重度 | 偏离内容 | 来源文件:行 | 是否有技术风险 | 建议处理步骤 |
|--------|---------|------------|--------------|------------|
| **低** | PresignedURL 代替 STS 临时凭证（Step 9） | `docs/progress/09_项目进度追踪.md:139` | 无，15min TTL 足够安全 | Step 13/18 可选升级 |
| **低** | AuditStatus=0 作为提交信号（Step 10） | `docs/progress/10_项目进度追踪.md:117` | 无，语义清晰 | 无需修改 |
| **低** | SHA256 字符串拼接代替二进制拼接（Step 11） | `docs/progress/11_项目进度追踪.md:102` | 无，Go stdlib 行为一致 | 无需修改 |
| **无** | Step 12 严格按 PRD 实现，无偏离 | `docs/progress/12_项目进度追踪.md:95` | — | — |

---

### 4.2 代码中的 MVP 临时策略

| 严重度 | 策略 | 文件:行 | 现状说明 | 建议处理步骤 |
|--------|------|---------|---------|------------|
| **高** | Channel EventBus（单机，无持久化） | `cmd/server/main.go:491-500` | `case "rabbitmq"` 返回 not-implemented 错误；Channel 模式在单进程重启时丢失所有未消费事件 | **Step 13**（核心目标） |
| **中** | `AddLike` 中 SADD + INCR 非原子 | `internal/cache/like_cache.go:122-130` | 代码注释已说明：两条命令之间有极小窗口可导致 count 多加。`RemoveLike` 已用 Lua（`like_cache.go:161`），存在不对称 | Step 13（补 Lua 脚本） |
| **低** | Mock 三件套（Audit / OSS / SMS） | `pkg/audit/mock.go`、`pkg/oss/mock.go`、`pkg/sms/mock.go` | dev 配置驱动，正确设计，永久保留 | 无需修改 |
| **低** | 空 `phone_key` 时明文存储 | `internal/service/user_service.go:177` | 仅 dev 环境触发，prod 配置强制非空 | 无需修改，Step 18 部署前检查 prod config |
| **低** | `is_visible=0` 默认 + 审核通过才可见 | `internal/model/post.go:50` | 正确的安全设计，永久保留 | 无需修改 |

---

## 5. 一致性问题

### 5.1 错误处理风格

**结论：全仓库一致，无违规。**

| 层 | 模式 | 抽检文件 |
|----|------|---------|
| Service 层 | 业务错误返回 `*errcode.AppError`，基础设施错误 wrap 后向上 | `user_service.go:34`、`like_service.go:90` |
| Handler 层 | `response.FromError(c, err)` 统一转换，无 switch-case 散落 | `post.go:74`、`like.go:~60` |
| Consumer 层 | 错误只 `zap.Warn`，不 panic，不停机 | `like_consumer.go:149,288` |

无 `fmt.Errorf` 直接返给 handler、无裸 `errors.New` 混用 AppError 的情况。

---

### 5.2 日志格式

**结论：全仓库统一使用 `zap.L()` 全局 logger，无混用。**

抽查无 `fmt.Println`、`log.Print`、`slog` 出现在 `internal/` 或 `pkg/`。

敏感字段脱敏（Step 12 完成）：
- 手机号：`pkg/crypto/mask.go` + `internal/middleware/logger.go`
- Token：middleware 日志中截断处理

---

### 5.3 ctx 透传

**结论：全链路一致，无断链。**

| 规则 | 验证点 | 状态 |
|------|-------|------|
| Handler → Service 传 `c.Request.Context()` | `internal/handler/post.go:72` | ✅ |
| Service → Repository 传 ctx | `user_service.go` 全部 repo 调用 | ✅ |
| Consumer 停机时用独立 `context.Background()` 做最终 flush | `like_consumer.go:240-245`（注释说明原因） | ✅ 正确 |
| 无 `context.TODO()` 残留 | 全仓库 grep 无命中 | ✅ |

---

### 5.4 CLAUDE.md 第七节模板套用实际遵守度

| 模板类型 | 参照模板 | 遵守度 | 备注 |
|---------|---------|--------|------|
| Cache | `user_cache.go` | ✅ 100% | Key 命名、TTL 显式、错误返回一致 |
| Consumer | `like_consumer.go` | ✅ 结构一致，⚠️ drainLoop 未抽取 | 见第 3 节 |
| Repository | `user_repository.go` | ✅ 100% | 接口先行、ErrXxxNotFound 封装一致 |
| Service | `user_service.go` | ✅ 100% | ctx 透传 + 日志格式 + 错误处理统一 |
| Handler | `user.go` | ✅ 100% | bind→validate→service→response 四步一致 |
| 外部依赖 | `pkg/sms/` | ✅ 100% | Audit / OSS / SMS 均为接口+Mock+Real 三件套 |

---

## 6. Step 13 前必须处理清单

### 🔴 严重度「高」条目（共 1 条）

| # | 严重度 | 问题 | 文件:行 | 行动 |
|---|--------|------|---------|------|
| H1 | **高** | Channel EventBus 无法在多实例（Docker 双 Go 实例）下工作，事件只在发布进程内消费，另一实例的 consumer 永远收不到 | `cmd/server/main.go:495-496` | **Step 13 核心目标**：实现 `internal/event/rabbitmq.go`，替换占位错误 |

> 说明：PRD `docker-compose` 部署是双 Go 实例（Step 3 已规划）。Channel 模式是单进程内通信，一旦两个实例同时运行，发布方实例的 consumer 消费，另一实例的 consumer 饥饿。Step 13 必须解决此项。

---

### 🟡 严重度「中」条目（Step 13 开工前建议处理）

| # | 严重度 | 问题 | 文件:行 | 行动 |
|---|--------|------|---------|------|
| M1 | **中** | Consumer 批量/冲刷参数（7 处）硬编码在源码，切 RabbitMQ 后调参需改代码 | `like_consumer.go:82,85,91`、`pv_consumer.go:57-58`、`count_consumer.go:85-86` | Step 13 开工前：新增 `MQConsumerConfig` 结构体，7 处常量改为从 config 读取 |
| M2 | **中** | Cache TTL（3 处）硬编码 | `like_cache.go:72`、`feed_cache.go:90,95` | Step 13 内：新增 `CacheConfig` 或扩展 `RedisConfig`，cache 构造函数注入 |
| M3 | **中** | `AddLike` SADD+INCR 非原子，与 `RemoveLike` 的 Lua 脚本不对称 | `internal/cache/like_cache.go:122-130` vs `like_cache.go:161` | Step 13 内：补 `AddLike` 的 Lua 脚本，消除不对称；RabbitMQ at-least-once 交付后重复事件概率增大，此问题从"极小窗口"变为"可观概率" |
| M4 | **中** | drainLoop 在 3 个 consumer 中复制粘贴，未抽取 | `like_consumer.go:226`、`pv_consumer.go:135`、`count_consumer.go:273` | Step 13 内：新增 RabbitMQ consumer 时顺手提取 `drainChannel[T]` helper，已有 3 处同步收编 |

---

### 🟢 已通过（无需处理）

| 维度 | 结论 |
|------|------|
| 跨层调用 | 无 handler→repo 直调 |
| 循环依赖 | 无 |
| EventBus 接口稳定性 | Topic 字符串常量可直接用作 RabbitMQ routing key，接口无需改动 |
| 错误处理一致性 | 全仓库统一 |
| 日志格式一致性 | 全仓库 zap.L()，无混用 |
| ctx 透传完整性 | 全链路无断链 |
| Repository 接口抽象 | 全部接口先行，service 无具体实现依赖 |
| 敏感数据脱敏 | Step 12 完成，手机号/token 均已处理 |

---

*本报告由 Claude Code 基于源码静态分析生成，无运行时 profiling。*
