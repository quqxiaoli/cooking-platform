# 项目进度追踪 · 烹饪内容分享平台

> 本文件是 Claude 与小离协作的**唯一权威进度账本**。每步收尾时由 Claude 全量重写、小离 review 后 commit。
>
> 偏离点章节是生成新版 PRD 的唯一权威依据。**Step 17 已完成，PRD v3.0 已生成**。
>
> 本文件记录 **Step 18 启动前的债务清理冲刺**（A/B/C 三组 15 条），不是 Step 18 本身。
> Step 18（服务器 + 域名 + HTTPS）会在债务清理之后单独立项。

---

## 总览

| 轮次           | 步骤                              | 状态      |
| ------------ | ------------------------------- | ------- |
| 第一轮 · 骨架跑通   | 1. 项目初始化                        | [x]     |
|              | 2. EventBus Channel 实现          | [x]     |
|              | 3. 用户模块                         | [x]     |
|              | 4. 内容模块                         | [x]     |
|              | 5. 点赞模块                         | [x]     |
|              | 6. 全链路验证 + PRD 收口               | [x]     |
| 第二轮 · 补齐功能   | 7. 搜索模块（MySQL FULLTEXT）         | [x]     |
|              | 8. 关注模块                         | [x]     |
|              | 9. 图片上传（OSS 直传）                | [x]     |
|              | 10. 内容审核 + 阿里云短信真实接入           | [x]     |
|              | 11. 手机号加密（AES-GCM + SHA256 迁移） | [x]     |
|              | 12. 日志脱敏 + 错误码体系最终收口           | [x]     |
| 第三轮 · 生产基础设施 | 13. EventBus 切换 RabbitMQ        | [x]     |
|              | 14. MySQL 主从 + DBResolver        | [x]     |
|              | 15. Nginx 双实例负载均衡               | [x]     |
|              | 16. Prometheus + Grafana          | [x]     |
|              | 17. CI/CD（GitHub Actions）+ PRD v3.0 | [x] |
| **预备 · 债务清理** | **Step 18 启动前债务清理（A/B/C 三组）** | **[x]** |
| 第四轮 · 部署上线   | 18. 服务器 + 域名 + HTTPS            | [ ]     |
|              | 19. 数据库迁移 + 种子数据                | [ ]     |
|              | 20. 公网验证 + 压测                   | [ ]     |

---

## 已完成的步骤详情

### 第 1–17 步

详见 `17_项目进度追踪.md`。

---

### 预备 · Step 18 启动前债务清理（2026-05-18）

进入第四轮（部署上线）之前，按 `docs/debt/09_三视图索引.md §4.1 / §4.2`
列出的高优清单，清理 **15 条债务**，分 A / B / C 三组顺序执行。
工作约束：不引新依赖、不动 DB schema、每文件改前告知、每组结尾跑
`make verify-step17 && make build && go test ./... -race`。

#### A 组 · P0 阻塞（7 条） ✅

| 编号 | 关联 TD | 一句话 |
|------|---------|--------|
| A1   | TD-INFRA-09 + TD-SYS-07 | `config.prod.yaml` 补齐 consumer / cache / metrics / search / follow / cors / audit-timeout 七段；新增 `scripts/check_config_parity.sh` + Makefile target |
| A2   | TD-INFRA-08 + TD-SYS-02 + TD-SYS-08 | health.go 改用 `response.Unavailable`；503001 别名 `ErrServiceUnavailable` 保留；errcode 头注释纠正 X=4/X=5 段位真相 |
| A3   | TD-SYS-04 | 三阶段 LIFO 关闭（5s / 15s / 5s），总预算来自 `cfg.Server.ShutdownTimeout`；`ConsumerManager.Shutdown(ctx)` 改带 ctx；新增 `closeWithDeadline` helper |
| A4   | TD-CRYPTO-01 | `EncryptPhone(keyHex="")` 改返 `ErrEmptyKey`；新增 errcode 480103；`config.Validate` release 模式拒绝空 key |
| A5   | TD-USER-03 / TD-SEARCH-01 / TD-FOLLOW-01 / TD-CORS-01 / TD-AUDIT-01 | Config-First 5 套补丁，全部从 const 提进 `cfg.*`；CORS 走 cfg 渲染，release + "*" 拒启动 |
| A6   | TD-PV-01 / TD-COUNT-01 / TD-SYS-10 (IDEMP-01) | 新增 `internal/cache/event_dedup_cache.go`（SETNX + TTL），PV / Count 在 subscribe handler 入口做 `SeenOrMark` 拦截 |
| A7   | TD-EVT-04 (DELIVERY-01) | `event/bus.go` Handler 注释加入 channel vs rabbitmq 失败语义对照表；channel.go / rabbitmq.go 写交叉引用 |

#### B 组 · prod 启动门面（3 条） ✅

| 编号 | 关联 TD | 一句话 |
|------|---------|--------|
| B1   | TD-INFRA-01 + TD-INFRA-02 + TD-METRICS-03 | 新增 `docker-compose.prod.yml`（restart=always / 无 host port 暴露 / app1-app2 走 cfg.prod.yaml / 全员 env_file）；新增 `.env.prod.example` 模板；`.gitignore` 改用 `.env*` + 白名单 |
| B2   | TD-INFRA-06 | `nginx.conf` 加 `client_max_body_size 6m`、`limit_req_zone api_zone 30r/s`、`limit_req_zone sms_zone 1r/m`、`limit_conn_zone`，并按 `/health` / `/api/v1/auth/send-code` / `/api/` / `/` 分四个 location 区别限流 |
| B3   | TD-METRICS-02 + TD-SYS-06 | `nginx.conf` 加 `location = /metrics { return 404; }`，公网入口屏蔽 metrics；Prometheus 仍走内网 scrape app1/app2 |

#### C 组 · CI / Ops（3 条） ✅

| 编号 | 关联 TD | 一句话 |
|------|---------|--------|
| C1   | TD-CI-01 | `pr.yml` 把 `staticcheck@latest` 锁到 `@2025.1.1`，附升级评审注释 |
| C2   | TD-CI-02 + TD-SYS-09 | `pr.yml` 在 staticcheck 后插入 `make verify-step17` 自检步骤 |
| C3   | TD-INFRA-04 | `init-slave.sh` 顶部加 `SHOW REPLICA STATUS` 探测：IO=Yes 且 SQL=Yes 直接 exit 0；其他状态走原 rebuild 流程 |

#### 子模块完成状态

- A 组（P0）✅
- B 组（prod 启动门面）✅
- C 组（CI / Ops）✅
- 收尾交付物（progress / storyline / changes / commit）✅

#### 产出文件

新增（5 个）：
- `docker-compose.prod.yml`
- `.env.prod.example`
- `internal/cache/event_dedup_cache.go`
- `scripts/check_config_parity.sh`
- `docs/debt/`（10 个台账文件 + 本步 3 个交付文档）

修改（27 个）：
- 配置层：`pkg/config/config.go` / `configs/{config,config.docker,config.prod}.yaml`
- 装配层：`cmd/server/main.go`
- 核心层：`pkg/errcode/errcode.go` / `pkg/crypto/phone.go` / `pkg/crypto/phone_test.go`
- 缓存层：`internal/cache/user_cache.go`
- 业务层：`internal/service/{follow,search,user}_service.go`
- 消费层：`internal/consumer/{pv,count}_consumer.go` / `internal/consumer/manager.go`
- 事件层：`internal/event/{bus,channel,rabbitmq}.go`
- 接入层：`internal/handler/health.go` / `internal/middleware/cors.go`
- 运维层：`deploy/nginx/nginx.conf` / `deploy/mysql/init-slave.sh`
- CI 层：`.github/workflows/pr.yml`
- 杂项：`.gitignore` / `Makefile` / `go.mod` / `go.sum`

#### 验证结果

```
make verify-step17               ✓
make build                       ✓ (go build ./... 无错)
make check-config-parity         ✓ (三 yaml 顶级 key 集合一致)
go vet ./...                     ✓
staticcheck ./...                ✓
go test ./... -race -count=1     ✓ （新加单元测试 phone_test.go ErrEmptyKey 路径）
```

#### 本步与 PRD 的偏离点

| PRD 章节 / 规则 | 实现差异 | 选择此方案的原因 |
|---|---|---|
| PRD v3.0 §10 已知技术债务 TD-PV-01 / TD-COUNT-01 原计划 "channel 类型扩展透传 EventID" | 改为 subscribe 边界 `SeenOrMark` 拦截，channel 类型保持 `chan int64` / `chan countDelta` 不动 | 在订阅边界处理重复事件，已 dedup 的事件不进 batch slot，效果等价于"透传 EventID 后再去重"，但代码改动面更小、不污染 channel 类型签名 |
| PRD v3.0 §6 错误码段位 X=4 含 470xxx / 480xxx | 头注释保留 X=4 历史段位，新增 503xxx 在 X=5；明确"HTTPStatus 才是单一权威，X 不是" | 不破坏既有 470/480 段位 ABI（GUARD：已发布 API 错误码定义不许动）；用注释而非重排段位修正历史口径 |
| PRD v3.0 §11 audit timeout 字段 | `AuditConfig.Timeout / MaxRetries` 在本步只加字段 + cfg validate + 三 yaml 段位，调用方 `pkg/audit/aliyun.go` 暂未接入 | 本步范围"债务清理"，不动 prod 行为；预留字段已被 cfg validate 卡住空值，后续 Step 18 起调用方一次性接入；偏离登记后无负担 |

#### 已 ✅ 清除的 TD 条目（共 16 条）

- 档 1：TD-INFRA-08 / TD-INFRA-09 / TD-CRYPTO-01 / TD-USER-03 / TD-SEARCH-01 / TD-FOLLOW-01 / TD-CORS-01 / TD-AUDIT-01 / TD-SYS-02 / TD-SYS-04 / TD-SYS-07 / TD-SYS-08
- 档 2：TD-METRICS-02 / TD-SYS-06 / TD-EVT-04（语义对照表落地后行为差异显式化）
- 档 4：TD-INFRA-01 / TD-INFRA-02 / TD-INFRA-04 / TD-INFRA-06 / TD-METRICS-03 / TD-CI-01 / TD-CI-02 / TD-SYS-09

> 注：PV / Count 的 IDEMP 仍归类 TD-PV-01 / TD-COUNT-01 / TD-SYS-10，本步通过 dedup 缓存关闭"必修"窗口，但 RabbitMQ 切换后第一周仍需要观察实测漂移再决定是否提级。

**下一步**：Step 18 · 服务器 + 域名 + HTTPS（第四轮 · 部署上线），可以直接使用 `docker-compose.prod.yml` + `.env.prod` 上线。

**PRD 状态**：`docs/prd/PRD-Final-v3.0.md` 已生成。本步 3 条偏离需要在 Step 20 收口时回灌进 PRD v3.0 的偏离点附录。

---
