# 项目进度追踪 · 烹饪内容分享平台

> 本文件是 Claude 与小离协作的**唯一权威进度账本**。每步收尾时由 Claude 全量重写、小离 review 后 commit。
>
> 偏离点章节是生成新版 PRD 的唯一权威依据。**Step 17 已完成，PRD v3.0 已生成。**

---

## 总览

| 轮次           | 步骤                              | 状态      |
| ------------ | ------------------------------- | ------- |
| 第一轮 · 骨架跑通   | 1. 项目初始化                        | [x]     |
|              | 2. EventBus Channel 实现          | [x]     |
|              | 3. 用户模块                         | [x]     |
|              | 4. 内容模块                         | [x]     |
|              | 5. 点赞模块                         | [x]     |
|              | 6. 全链路验证 + PRD 收口               | [x]     |
| 第二轮 · 补齐功能   | 7. 搜索模块（MySQL FULLTEXT）         | [x]     |
|              | 8. 关注模块                         | [x]     |
|              | 9. 图片上传（OSS 直传）                | [x]     |
|              | 10. 内容审核 + 阿里云短信真实接入           | [x]     |
|              | 11. 手机号加密（AES-GCM + SHA256 迁移） | [x]     |
|              | 12. 日志脱敏 + 错误码体系最终收口           | [x]     |
| 第三轮 · 生产基础设施 | 13. EventBus 切换 RabbitMQ        | [x]     |
|              | 14. MySQL 主从 + DBResolver        | [x]     |
|              | 15. Nginx 双实例负载均衡               | [x]     |
|              | 16. Prometheus + Grafana          | [x]     |
|              | **17. CI/CD（GitHub Actions）+ PRD v3.0** | **[x]** |
| 第四轮 · 部署上线   | 18. 服务器 + 域名 + HTTPS            | [ ]     |
|              | 19. 数据库迁移 + 种子数据                | [ ]     |
|              | 20. 公网验证 + 压测                   | [ ]     |

---

## 已完成的步骤详情

### 第 1–16 步

详见 `16_项目进度追踪.md`。

---

### 第 17 步：GitHub Actions CI/CD + PRD v3.0 生成（2026-05-17）

**子模块完成状态**：

- CI/CD 基础设施（3 个 workflow 文件）✅
- 集成测试（MySQL + Redis ping）✅
- verify_step17.sh（8 段全通过）✅
- Makefile verify-step17 ✅
- PRD v3.0 生成（`docs/prd/PRD-Final-v3.0.md`）✅
- 故事线 + 代码变更清单 ✅

**产出文件**：

新增：
- `.github/workflows/pr.yml`（PR 触发：go vet + go test + staticcheck，services: MySQL+Redis）
- `.github/workflows/build.yml`（push main：go build + Docker smoke build）
- `.github/workflows/release.yml`（tag push v*：build + push to GHCR）
- `internal/integration/ping_test.go`（TestMySQLPing + TestRedisPing，env-var skip）
- `scripts/verify_step17.sh`（8 段验证，全部通过）
- `docs/prd/PRD-Final-v3.0.md`（最终技术文档，含 13 章节）

修改：
- `Makefile`（追加 verify-step17 target + .PHONY）

**关键决策**：

1. **CI 方案 B（真实 services）**：在 GitHub Actions 中起 MySQL 8.0 + Redis 7.2 Docker services，集成测试在 CI 环境自动跑，本地未设 `TEST_MYSQL_DSN` / `TEST_REDIS_ADDR` 时 t.Skip() 跳过。比 `-short` 方案更规范，且留下了"CI 跑真实数据库测试"的面试故事点。

2. **发布到 GHCR**：使用 `docker/build-push-action@v5` + `docker/metadata-action@v5`，镜像 tag 自动从 git tag 提取（semver），同时打 latest。`${{ secrets.GITHUB_TOKEN }}` 无需额外配置 secret。

3. **go-version-file: go.mod**：不在 workflow 里硬写 Go 版本，自动从 go.mod 读取，版本升级时只改一处。

4. **PRD v3.0 生成策略**：整合 Step 9–16 全部偏离点（13 条）+ 架构体检 5 项问题修复结论 + 错误码全表 + 配置项全表 + Redis Key 全表 + 监控 SLO + 部署 Runbook + 安全清单 + 已知技术债务表。

**验证结果**：

```
§1 Workflow 文件存在性检查                           ✓
§2 YAML 语法验证（pr/build/release）                  ✓
§3 Workflow 触发器验证                               ✓
§4 pr.yml services 块（MySQL + Redis）               ✓
§5 go build + go vet 本地验证                        ✓
§6 集成测试文件存在（TestMySQLPing / TestRedisPing）   ✓
§7 单元测试（middleware / event / crypto）全部通过     ✓
§8 集成测试本地正确 SKIP                              ✓
```

**本步与 PRD 的偏离点**：

| PRD 章节 / 规则 | 实现差异 | 选择此方案的原因 |
|---|---|---|
| conventions §2 Step17 "go test ./..." | 改为 Method B：GitHub Actions services: MySQL+Redis + env-var skip | 更规范，保留集成测试基础设施，面试价值更高 |

**下一步**：Step 18 · 服务器 + 域名 + HTTPS（第四轮 · 部署上线）

**PRD 状态**：`docs/prd/PRD-Final-v3.0.md` 已生成，是 Step 18–20 的唯一权威参考。

---
