# 项目进度追踪 · 烹饪内容分享平台

> 本文件记录第四轮（部署上线）Step 18 完成、Step 19 进行中受阻的状态。
> Step 19 卡在一个未解决的技术问题，详见配套文件
> `19_故障诊断与交接-Viper环境变量覆盖.md`。

---

## 总览

| 轮次 | 步骤 | 状态 |
| --- | --- | --- |
| 第一轮~第三轮 + 债务清理 | 1–17 + Step18 前债务清理 | [x] 详见 17/18 进度文件 |
| 第四轮 · 部署上线 | **18. 服务器 + 域名 + HTTPS** | **[x]（HTTPS 部分待域名审核）** |
|  | **19. 数据库迁移 + 种子数据** | **[~] 进行中 · 受阻（app 无法启动）** |
|  | 20. 公网验证 + 压测 | [ ] 未开始 |

---

## Step 18：服务器 + 域名 + HTTPS（2026-05-19）

### 已完成 ✅

| 项 | 结果 |
| --- | --- |
| 云服务器 | 阿里云香港 ECS，2核4G，Ubuntu 22.04，公网 IP `47.238.29.251`，300 元免费额度抵扣 |
| SSH 登录 | 密码方式（用户 root，密码自存本地），Mac 本地终端可登录 |
| 安全组 | 入方向开放 22 / 80 / 443，源 `0.0.0.0/0`；建议删 3389（未确认是否已删） |
| Docker 环境 | Docker 29.5.1 + Compose v5.1.3，hello-world 验证通过，Docker Hub 可拉镜像 |
| 代码拉取 | GitHub Private 仓库经 Deploy Key（只读）clone 到 `/opt/cooking-platform`，commit `065ca56`（main 最新，与 18_进度文件一致） |
| OSS 真实接入 | Bucket `cooking-platform-prod`（香港，endpoint `oss-cn-hongkong.aliyuncs.com`），公共读 + 关闭阻止公共访问 + CORS（GET/PUT/POST、`*`、ETag、600、Vary:Origin），RAM 子账号 `cooking-platform-oss` 授权 `AliyunOSSFullAccess`，AK/SK 已获取并自存本地 |
| `.env.prod` | 16 项全部填写并通过完整性校验（占位符仅剩 SMS/AUDIT 4 行——mock 故意保留；加密 key 长度 64；DSN 用户名 cooking 非 root；MQ URL 保留 `${}` 字面量）。已确认被 .gitignore 正确忽略，零泄密 |
| SMS/审核决策 | 决定 1：SMS 与内容审核 prod 保留 mock provider，面试讲"三件套抽象 + 生产可切换"。**注意：此改动随排查回退已被撤销，需重做（见下方遗留）** |
| 基础设施容器 | mysql / mysql-slave / redis / rabbitmq 四个容器全部 `Up (healthy)` |

### 受阻/待办 ⏳

| 项 | 状态 |
| --- | --- |
| 域名 `laidbackck.cn` | 信息模板"注册局审核中"，域名尚未注册。并行等待，不阻塞服务器初始化 |
| DNS 解析 + HTTPS | 待域名审核通过后做 |
| 安全组删 3389 | 建议执行，未确认 |

---

## Step 19：数据库迁移 + 种子数据（2026-05-19，进行中受阻）

### 已完成 ✅

| 项 | 结果 |
| --- | --- |
| migrate CLI | golang-migrate v4.17.1 装到服务器 `/usr/local/bin/migrate` |
| 业务用户 | `cooking`@`%` 已建，库级最小权限：`cooking_platform.*` 上 SELECT/INSERT/UPDATE/DELETE/CREATE/DROP/REFERENCES/INDEX/ALTER；全局仅 USAGE（无实权）；无 GRANT/FILE/SUPER |
| 数据库 | `cooking_platform`（utf8mb4_unicode_ci）已建 |
| 迁移执行 | 6 个迁移全部 applied（000001~000006：users/posts/likes/follows/post_steps/audit_logs），`schema_migrations` version=6 dirty=0，7 张表齐全。**用 cooking 用户跑通，顺带验证最小权限足够** |
| 种子数据决策 | **不做**。`seed.sh` 本就是空壳；scene_tag 是字段约定非预置行；生产库应空，由真实用户填充。已确认为偏离点（见下方） |
| 主从模式决策 | 本步先单主模式（slaves_dsn 空）。`.env.prod` 只有单个 master DSN；读写分离启用留待后续。偏离点（见下方） |

### 受阻 🔴 —— app1/app2 无法启动

**核心症状**：基础设施 4 容器全 healthy，但 app1/app2 持续 `Restarting (1)`，服务无法对外提供。

**问题已隔离到**：app 加载 `config.prod.yaml`（prod 配置）路径失败；加载 dev `config.yaml` 时一切正常。根本症结是 `APP_DATABASE_DSN` 环境变量无法覆盖 `config.prod.yaml` 里 `database.dsn` 的占位符（Viper AutomaticEnv 对嵌套 key 的已知不可靠行为）。

详细诊断、已确证事实清单、失败的修复尝试、当前 git 状态、下一步建议——全部记录在配套文件：

**`19_故障诊断与交接-Viper环境变量覆盖.md`**

### 当前代码状态（重要）

本窗口曾用 python 脚本注入修复（config.go 加 12 行 BindEnv + config.prod.yaml dsn 改空 + sms/audit 改 mock），因引发"零输出 exit 1"难以定位，**已全部 `git checkout` 撤回**。

现状：`git status` = working tree clean，完全回到 commit `065ca56` 原始状态。`.env.prod`（gitignore，未受影响）保留。Docker 镜像 `cooking-platform-app1` 仍是含已撤回改动的旧 build，**下个窗口需在干净代码上重新 build**。

---

## 本步与 PRD 的偏离点

| PRD 章节 / 规则 | 实现差异 | 选择此方案的原因 |
| --- | --- | --- |
| 落地顺序 Step 19「数据库迁移 + 种子数据」 | 不做种子数据，prod 保持空库 | `seed.sh` 自 Step 4 起即空壳从未实现；scene_tag 1-8 是 posts 表字段 COMMENT 约定，非预置行，无 tags 表；面向公网的真实产品生产库应为空，由真实用户注册填充；灌假数据会污染真实环境、扭曲监控 |
| PRD-Phase3 Step 14 主从读写分离 | 本步 prod 先走单主模式（config slaves_dsn 空），所有读写走 master | `.env.prod` 仅注入单个 `APP_DATABASE_DSN`（master）；启用 DBResolver 接 slave 需单独 slave DSN 注入；本步目标"服务跑通"，单主模式 app 功能完整；读写分离可后续补 slave DSN env 启用 |
| 决定 1：SMS/审核 prod 用 mock | 计划改 config.prod.yaml 三个 provider，但随 git 回退被撤销，**尚未正式落地** | 需在干净基线上用正确方式（非 python 乱改）重做；这是 Step 19/20 收尾前必须重新落实并登记的项 |

---

## 下一步

1. **优先解决 Step 19 阻塞**：按 `19_故障诊断与交接` 文件的"恢复计划"，从 git 干净基线重新 build 镜像、干净观测、用正确方式修复 Viper env 覆盖问题
2. app1/app2 healthy 后：验证 `curl localhost/health` → `curl 47.238.29.251/health` 公网可访问
3. 重新落实「决定 1」（SMS/audit prod = mock），并登记偏离点
4. Step 19 真正完成后再正式收尾交付，补 Step 20
5. 域名审核通过后回到 Step 18 收尾：DNS 解析 + Let's Encrypt HTTPS

---

## 特殊提醒

- 本窗口 DB 密码、root 密码、OSS AK/SK 等敏感值均由用户自存本地，Claude 未接触
- 服务器 bash history 中出现过 DB 密码明文（migrate / mysql -p 命令行），上线前建议清理：`history -c && cat /dev/null > ~/.bash_history`
- `.env.prod` 已确认被 .gitignore 忽略，未进入 git 跟踪
- 本窗口未做任何 git commit / push（所有改动已 checkout 撤回，仓库 = 远端 main）
- Docker 旧镜像残留：`cooking-platform-app1`（含已撤回代码）+ `cooking-build-test`（测试镜像）需在下个窗口重新 build 前注意清理或 --no-cache
